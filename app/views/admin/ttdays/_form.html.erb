<%= form_with(model: [:admin,ttday], local: true) do |form| %>
  <% if ttday.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ttday.errors.count, "error") %> prohibited this ttday from being saved:</h2>

      <ul>
      <% ttday.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :ttday_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
